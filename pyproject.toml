[project]
name = "digital-human-world"
version = "0.1.0"
description = "Prototype loop for a persistent digital human world."
requires-python = ">=3.11"

[tool.digital-human-world]
test-command = "python -m unittest discover -v"
validation-command = "python validate_v1.py"

